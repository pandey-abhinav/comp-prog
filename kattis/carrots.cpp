#include <bits/stdc++.h>

using namespace std;

int main(void) {
  int n, p;
  scanf("%d %d", &n, &p);
  char s[1001];
  for (int i = 0; i < n; i++) {
    scanf("%*s");
  }
  printf("%d\n", p);
  return 0;
}
